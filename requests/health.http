### Health check — no requiere auth
@baseUrl = http://localhost:3000

### Root
GET {{baseUrl}}/

### DB health
GET {{baseUrl}}/health/db

### Documento OpenAPI en crudo — sin auth, solo fuera de producción
### Lo consume `openapi-typescript` del front para generar sus tipos.
GET {{baseUrl}}/api-docs.json
